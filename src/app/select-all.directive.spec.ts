import { SelectAllDirective } from './select-all.directive';

describe('SelectAllDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectAllDirective();
    expect(directive).toBeTruthy();
  });
});
